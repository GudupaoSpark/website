---
import AboutComponent from '../components/About.astro';
import Layout from '../layouts/Layout.astro';
import { getTranslator } from '@gudupao/astro-i18n';
const lang = Astro.params.lang || 'en';
const t = getTranslator(lang);
---

<Layout title={t('about.page_title') || `${t('header.about')} | Gudupao`}>
	<AboutComponent />
</Layout>

<script>
	// Ensure SPA navigation works for the about page
	document.addEventListener('astro:page-load', () => {
		// Page loaded
	});
</script>
